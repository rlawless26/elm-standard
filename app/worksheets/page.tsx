import PageStub from "@/components/PageStub";

export const metadata = { title: "Worksheets — Elm Standard" };

export default function WorksheetsPage() {
  return (
    <PageStub
      eyebrow="PRINTABLES"
      title="Print, measure, send."
    />
  );
}
