import PageStub from "@/components/PageStub";

export const metadata = { title: "Get a quote — Elm Standard" };

export default function QuotePage() {
  return (
    <PageStub
      eyebrow="GET A QUOTE"
      title="Tell me about the radiator. I'll send a number."
    />
  );
}
