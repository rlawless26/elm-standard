import PageStub from "@/components/PageStub";

export const metadata = { title: "Elm Standard — Custom radiator covers" };

export default function HomePage() {
  return (
    <PageStub
      eyebrow="MILTON, MA"
      title="Custom radiator covers, handbuilt to order."
      lede="One pair of hands, one shop. Local install or flat-pack ship — three styles, exact-fit, anchored to the wall."
    />
  );
}
