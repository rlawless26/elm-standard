import PageStub from "@/components/PageStub";

export const metadata = { title: "Tip safety — Elm Standard" };

export default function SafetyPage() {
  return (
    <PageStub
      eyebrow="SAFETY"
      title="Anchored to the wall. Stable on the floor."
    />
  );
}
